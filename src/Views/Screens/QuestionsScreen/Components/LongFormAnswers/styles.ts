import styled from 'styled-components/native';
import {APP_COLORS} from '../../../../../AppStyles';

export const StyledTextInput = styled.TextInput`
  border-color: ${APP_COLORS.black};
  border-width: 1px;
  padding: 10px;
  margin: 10px 0px;
`;

export const TempratureText = styled.Text`
  padding-top: 10px;
`;
